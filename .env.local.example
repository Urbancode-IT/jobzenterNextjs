# Copy this file to .env.local and adjust if needed.
# Database: PostgreSQL (pgAdmin) – Jobzenter, port 5434

DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=admin@123
DB_NAME=Jobzenter

# Contact form emails (Nodemailer). Admin gets new submissions; student gets confirmation.
ADMIN_EMAIL=jobzenter24@gmail.com
# FROM_EMAIL and FROM_NAME used as sender (defaults to SMTP_USER and "Jobzenter")
# FROM_EMAIL=
# FROM_NAME=Jobzenter

# SMTP (e.g. Gmail: use App Password, not account password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jobzenter24@gmail.com
SMTP_PASSWORD=lvcx tjvx chtc ihf
